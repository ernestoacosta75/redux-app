/**
 * The principle is the same: we create higher-order functions
 * which produce reducers.
 *
 * Similar to the higher-order action creator, we accept a mapping
 * of async states and action types, except this time, for convenience,
 * in reverse.
 */

 // Then, we yield a reducer
